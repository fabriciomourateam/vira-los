# ── Servidor ─────────────────────────────────────────────────────────────────
PORT=3001
FRONTEND_URL=http://localhost:8080

# URL pública do servidor (obrigatório para Instagram — use ngrok localmente)
# Ex local:    SERVER_URL=https://seu-id.ngrok.io
# Ex produção: SERVER_URL=https://seu-app.railway.app
SERVER_URL=http://localhost:3001

# ── Instagram Graph API ───────────────────────────────────────────────────────
# Crie em: https://developers.facebook.com → Meu App → Configurações Básicas
# Precisa de: conta Instagram Business/Creator vinculada a uma Página do Facebook
INSTAGRAM_APP_ID=
INSTAGRAM_APP_SECRET=
INSTAGRAM_REDIRECT_URI=http://localhost:3001/api/platforms/instagram/callback

# ── TikTok Content Posting API v2 ────────────────────────────────────────────
# Crie em: https://developers.tiktok.com → Manage Apps
# Escopos necessários: user.info.basic, video.upload, video.publish
TIKTOK_CLIENT_KEY=
TIKTOK_CLIENT_SECRET=
TIKTOK_REDIRECT_URI=http://localhost:3001/api/platforms/tiktok/callback

# ── YouTube Data API v3 ───────────────────────────────────────────────────────
# Crie em: https://console.cloud.google.com → APIs & Services → Credenciais
# Ative: YouTube Data API v3
YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=
YOUTUBE_REDIRECT_URI=http://localhost:3001/api/platforms/youtube/callback

# ── Unsplash API ──────────────────────────────────────────────────────────────
# Crie em: https://unsplash.com/developers → New Application
# Usado pelo gerador de carrossel para buscar imagens retratos
UNSPLASH_ACCESS_KEY=
UNSPLASH_SECRET_KEY=
UNSPLASH_APP_ID=

# ── Pexels API ────────────────────────────────────────────────────────────────
# Crie em: https://www.pexels.com/api/ → Get Started (gratuito)
# A chave é usada diretamente como header Authorization.
# Usado em dois lugares:
#   1. Fallback de imagens do gerador Básico (quando Unsplash falhar)
#   2. Fonte primária da Máquina de Carrosséis — proxy CORS-free em /api/pexels
#      e substituição automática de placeholders PEXELS:query no HTML gerado.
PEXELS_API_KEY=

# ── John Hulk — job diário separado (reel de referência → carrossel FMTeam) ───
# Reaproveita as chaves que já existem no projeto: APIFY_API_KEY (scraping do
# perfil/reel), OPENAI_API_KEY (transcrição Whisper), ANTHROPIC_API_KEY (tema +
# carrossel), UNSPLASH_ACCESS_KEY/PEXELS_API_KEY (imagens), IMAGE_PROVIDER
# (reserva de IA quando stock não acha), DAILY_CRON_SECRET (protege o disparo
# do cron em /api/john-hulk/generate, mesma chave do /api/daily-content).
# As 3 abaixo são opcionais — sem elas, os defaults já funcionam.

# Perfil(is) do Instagram usado(s) como referência (CSV — rotaciona 1/dia por
# dia-do-ano se houver mais de um). JOHN_HULK_HANDLE (singular) é aceito por
# compatibilidade se JOHN_HULK_HANDLES não estiver setada.
JOHN_HULK_HANDLES=john.hulk_
JOHN_HULK_HANDLE=john.hulk_

# Só considera reels publicados há pelo menos N dias (evita usar conteúdo
# "quente demais" do perfil de referência). Default: 7.
JOHN_HULK_OFFSET_DAYS=7

# Webhook opcional (Discord/Slack/Zapier) que recebe um POST JSON quando um
# rascunho fica pronto pra revisão ({event, topic, reelUrl, carouselId, at}).
# Sem isso setado, o job só loga no console do servidor.
JOHN_HULK_NOTIFY_WEBHOOK=
